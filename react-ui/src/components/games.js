var React = require('react');
const Game = require('./game-search');

class Game extends React.Component {
    render() {
        return(
            <div>
            
            </div>
            )
    }
}

module.exports = Game;